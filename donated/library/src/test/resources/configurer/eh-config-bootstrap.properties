failOnError = false

enable.reload = false

config.order=configurer, override

config.configurer.location=configurer

config.override.type=FileSystem
config.override.location=/data/configTest
config.override.name=configurer-override
